package ru.mobnius.core.data;

import android.os.Bundle;

//import org.greenrobot.greendao.AbstractDaoSession;

public interface OnFormValuesListeners {
    /**
     * Получение данных для формы
     * @param session сессия
     * @param routeId иден. маршрута
     * @param pointId иден. точки мартрута
     * @param resultId иден. результата
     */
    ///Bundle onFormValues(AbstractDaoSession session, String routeId, String pointId, String resultId);
}
