/*package ru.mobnius.cic.data.manager;

import androidx.test.ext.junit.runners.AndroidJUnit4;

import org.junit.Test;
import org.junit.runner.RunWith;

import java.util.List;

import ru.mobnius.core.data.GlobalSettings;
import ru.mobnius.core.data.configuration.ConfigurationSetting;
import ru.mobnius.core.data.configuration.ConfigurationSettingUtil;
import ru.mobnius.cic.ManagerGenerate;

import static org.junit.Assert.assertTrue;

@RunWith(AndroidJUnit4.class)
public class ConfigurationSettingUtilTest extends ManagerGenerate {
    @Test
    public void getSettings() {
        List<ConfigurationSetting> configurationSettings = ConfigurationSettingUtil.getSettings(GlobalSettings.getConnectUrl(), getCredentials());
        assert configurationSettings != null;
        assertTrue(configurationSettings.size() > 0);
    }
}

*/