<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/point_info_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"/>

    <ProgressBar
        android:id="@+id/osm_point_info_progress"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="@id/osm_point_info_list_map"
        app:layout_constraintBottom_toBottomOf="@id/osm_point_info_list_map"
        android:visibility="gone"
        tools:visibility="visible"
        android:elevation="@dimen/indent_8"/>

    <org.osmdroid.views.MapView android:id="@+id/osm_point_info_list_map"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:minHeight="200dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/point_info_list"
        android:visibility="gone"
        tools:visibility="visible" />

    <!--androidx.fragment.app.FragmentContainerView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:minHeight="200dp"
        android:id="@+id/point_info_list_map"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/osm_point_info_list_map"
        android:name="com.google.android.gms.maps.SupportMapFragment" /-->

</androidx.constraintlayout.widget.ConstraintLayout>